# Primeira-aula-DevOps
# Aprendendo a usar

#lista = list(range(1,101))
  #def contaImpar(lista):
    #count=0
    #for x in lista:
        #print(x)
        #if x %2 != 0:
             #count += 1
        #print(count)

#contaImpar(lista)
